name: CI/CD Pipeline

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build-and-test:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.12'

    - name: Install dependencies
      run: pip install -r requirements.txt

    - name: Run Linting (Flake8)
      run: flake8 src

    - name: Run Security Scan (Bandit)
      run: bandit -r src

    - name: Run Tests (Pytest)
      run: pytest

  build-docker-image:
    runs-on: ubuntu-latest
    needs: build-and-test
    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Build Docker image
      run: docker build -t subscription-service-backend .

    - name: Run Trivy vulnerability scan on Docker image
      uses: aquasecurity/trivy-action@master
      with:
        image-ref: 'subscription-service-backend'
        format: 'table'
        exit-code: '1'
        severity: 'CRITICAL,HIGH'

    # Add steps for pushing to a container registry and deployment here
    # - name: Log in to Docker Hub
    #   uses: docker/login-action@v2
    #   with:
    #     username: ${{ secrets.DOCKER_USERNAME }}
    #     password: ${{ secrets.DOCKER_PASSWORD }}
    # - name: Push Docker image
    #   run: docker push your-docker-username/subscription-service-backend:latest
